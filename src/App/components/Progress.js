import React from 'react';
import AppBar from './AppBar';

const Progress = () => {
  const title = 'Progress Report';

  return (
    <>
      <AppBar title={title} />
    </>
  );
};

export default Progress;
